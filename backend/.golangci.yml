run:
  timeout: 5m
  skip-dirs:
    - internal/server # Temporarily skip until Gin is integrated

linters:
  enable:
    - gofmt
    - goimports
    - revive
    - govet
    - staticcheck
    - unused
    - errcheck

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
