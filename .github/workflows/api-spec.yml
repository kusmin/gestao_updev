name: API Spec Quality

on:
  push:
    paths:
      - docs/api.yaml
      - .github/workflows/api-spec.yml
  pull_request:
    paths:
      - docs/api.yaml
      - .github/workflows/api-spec.yml

permissions:
  contents: read

jobs:
  spectral-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run Spectral lint
        run: npx @stoplight/spectral-cli lint docs/api.yaml

      - name: Build Redoc HTML
        run: npx @redocly/cli build-docs docs/api.yaml --output api-reference.html

      - name: Upload API artifact
        uses: actions/upload-artifact@v4
        with:
          name: api-reference
          path: api-reference.html

  deploy-pages:
    needs: spectral-lint
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Redoc HTML
        run: |
          mkdir -p public
          npx @redocly/cli build-docs docs/api.yaml --output public/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload HTML artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
