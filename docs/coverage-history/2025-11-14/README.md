# Cobertura – 14/11/2025

Relatórios gerados via `make coverage`.

- **Backend (Go):** 17.0% de statements (`backend/coverage.out` e `backend.coverage.html`).
- **Frontend (Vitest):** 31.63% de statements (`frontend.lcov.info`).
- **Backoffice (Vitest):** 0% – ainda não há testes (`backoffice.lcov.info`).
- **E2E:** 8 fluxos Chromium/Firefox (ver `e2e.md`).

Os artefatos desta data podem ser usados como linha de base para acompanhar a evolução descrita em `docs/plano-cobertura-codigo.md`.
